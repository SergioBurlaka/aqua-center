import { createQueryKeys } from '@lukemorales/query-key-factory';

export const usersVersionsKeys = createQueryKeys('usersVersions', {
  usersVersions: null,
  versionList: null,
  editUserVersion: null,
});
